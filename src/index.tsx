import * as React from "react";
import * as ReactDOM from "react-dom";

import { Number_pad } from "./components/number_pad";

ReactDOM.render(<Number_pad />,
    document.getElementById("maincontainer")
);